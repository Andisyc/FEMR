# FRS-v015 Task Canvas

Status: active volatile control surface. Updated: 2026-07-21.

## Objective

Implement the accepted Stage-3 local-repair experiment without mixing its four
separate objects: current root artifact, future q29 intent, Clean GMT
continuation, and one-action K evidence.

## Method Control Surface

- Concept Figure: `../../architecture/concept/03_frontres_concept_tabs.data.json`
- Concept Figure update owner: no update pending; v015 mapping is accepted.
- Detailed design-point register: `../contracts/README.md#concept-figure-design-point-register`
- Active contracts: Method v015, Training v007, Gain v003, PPO v003, Eval v003.

## Current Step

`R6 / 7 partial at E-FI-24; telemetry and remote preflight pass, source-transfer authorization is required before the single S4 transaction`

Status: the earlier local scenario, one-action K, Gain, grouped transaction, and
persistence evidence remains valid at its stated local/fake tiers. R1--R3 close
the deterministic current-command, role-aligned q29 append, and actor/GMT
authority defects reopened by R0. R5 exercises the actual observation owner;
R6-S0 adds a fail-closed structured live snapshot. No method or Concept Figure
change is pending.

Scope:
- retain the completed proposal-only HSL boundary, sealed two-role reset,
  one-action K candidate collector, v003 Gain consumers, diagnostics, and the
  sealed v015 metadata/grouped candidate adapter;
- retain the completed fake S2 connectivity for sealed transaction validation,
  q29 actor tail before normalizer, grouped-loss consumption, and exactly one
  step (`E-FI-14`);
- retain the completed CPU-fake S3 checkpoint identity and atomicity boundary:
  exact q29 H layout/prefix stats, no partial persistence, and committed-receipt
  only resume (`E-FI-22`, rebasing the generic-dimension `E-FI-15` fixture).

Non-scope:
- HSL changes, grouped-formula changes, Concept Figure changes, simulator,
  real training, another live transaction, and deployment composition.

Step 5A-S1 preflight boundary:
- use a cold-start fresh v015 FrontRES policy for identity testing; do not load
  an old HSL checkpoint;
- use two fresh UNKNOWN Segments with minimum M=2, giving four Repair rows and
  four Noisy rows (`num_envs=8`);
- execute only on SUST_Main_2 where `/hdd1/cyx/MOSAIC/model/model_27000.pt`,
  `/hdd1/cyx/AMASS_G1Segment`, and `/hdd1/cyx/AMASS_G1NPZ_Final` are present.

## Completed

- `R5 / 7`: actual current command -> five-frame history -> raw `870D` -> q29
  `58D` -> normalized `928D` -> FEMR `158D` / GMT `770D` -> one-action K ->
  four grouped attempts -> exact-one update is offline-S2 contract-confirmed;
  Clean C advances before each fresh GMT observation (`E-FI-23`).
- `R4 / 7`: v015 checkpoint v2 binds `(1,2)`, `928/158/770`, the complete
  `158D` prefix-stat fingerprint, and committed-only transaction receipt;
  v1/full/zero/65D/unversioned/partial identities reject before mutation
  (`E-FI-22`).
- `R3 / 7`: v015 config/runner/model resolves `870+58=928`, exposes only the
  first `158D` to FEMR, preserves the final `770D` for frozen GMT, and rejects
  `num_frontres_obs=0` (`E-FI-21`).
- `R2 / 7`: command-owned read-only intent snapshot, B=8 role-aligned offsets
  `(1,2)` -> `[8,58]`, B=4 policy-batch isolation, permutation/provenance and
  forbidden-field rejection (`E-FI-20`).
- `R1 / 7`: role-aligned current deployment q29/dq29 command at `t`, strict
  horizon/velocity/provenance checks, and unchanged K-only Clean C (`E-FI-19`).
- `R0 / 7`: formal observation contract frozen, Step 1B/5A-S0 evidence
  coverage corrected, and R1 command-owner boundary recorded (`E-FI-18`).
- `G0`: accepted semantics and Concept Figure/contracts synchronized (`E-FI-0`).
- `G0`: read-only white-box owner audit and refined twelve-step plan (`E-FI-1`).
- `G1 / Step 1A`: immutable local scenario schema, q29 invariant, hash,
  provenance split, and legacy-tape rejection (`E-FI-2`).
- `G1 / Step 1B`: isolated versioned q29-tail builder, provenance/clean
  isolation, and legacy-layout rejection (`E-FI-3`); formal role alignment and
  actor visibility are reopened by `E-FI-18`.
- `Gate H0`: HSL source/layout/target and checkpoint boundary mapped (`E-FI-4`).
- `H0-A`: proposal-only HSL authority confirmed and Training v007 activated
  (`E-FI-5`); no source migration is implied.
- `H1-S1a`: sealed-q29 Stage-1 input/current anti-DR target and Stage-3
  legacy/direct/loss/checkpoint rejection (`E-FI-6`).
- `H1-S2`: CPU-only fake q29 -> normalizer -> actor -> current-target and
  zero-writer -> storage -> batch -> zero-HSL-loss connectivity (`E-FI-7`).
- `G2 / Step 2A`: v015 Repair/Noisy-only layout, Clean `x_t` dynamic reset,
  immutable local command carrier, retry identity, and legacy-route rejection
  under deterministic fake reset (`E-FI-8`).
- `G2 / Step 2B`: exactly one t action/statistics tuple, frozen later FEMR,
  Clean-C command cursor, K-valid evidence, immutable M-attempt re-arm, and
  reset -> actor -> GMT -> carrier fake connectivity (`E-FI-9`).
- `G3 / Step 3A`: typed root-free q29 intent/paired-physics/full-6D-cost pure
  Gain owner, explicit provenance rejection, no-op protection, and
  UNCONFIRMED optional components (`E-FI-10`).
- `G3 / Step 3B`: post-`t` robot q29/typed `I[t]` candidate capture, sole v003
  Gain invocation, one-row return/advantage carrier, and immutable
  scenario-keyed priority evidence with v002 rejection (`E-FI-11`).
- `G3 / Step 3C`: v003-only local-K diagnostic projection, explicit legacy
  evaluator rejection for v015, and a separate no-feedback deployment-
  composition protocol (`E-FI-12`).
- `G4 / Step 4A`: immutable v015 transaction metadata, complete grouped
  candidate adapter, one-row/K-evidence mass isolation, and legacy-adapter
  rejection under deterministic tests (`E-FI-13`).
- `G4 / Step 4B`: CPU fake formal transaction plan/accumulator, q29-only actor
  route, grouped v003 loss, and exact-one explicit optimizer update with
  HSL/legacy/partial fail-closed isolation (`E-FI-14`).
- `G4 / Step 4C`: CPU fake persistence owner, exact q29 H/prefix-normalizer/
  grouped-loss identity, in-flight save/resume rejection, and committed-receipt
  only resume (`E-FI-15`).
- `G5 / Step 5A-S0`: explicit v015-only config/entrypoint, sealed local-scenario
  provider, split reset carrier, one-action-K candidate connection, and existing
  grouped exact-one transaction owner under deterministic CPU-only evidence
  (`E-FI-16`).

## Active Documents

- `FRS-v015-future-intent-single-action-k-engineering-plan.md`
- `../checklists/FRS-v015-future-intent-single-action-k-checklist.md`
- `../../testing/evidence_ledger_v015_future_intent_single_action_k_2026-07-19.md`

## Verified Evidence

- `E-FI-0`: user-confirmed local semantics and v015 active-contract closure.
- `E-FI-1`: current materializer is 65D tape-based; runtime H bridge expects
  65D; reset is quartet-oriented; Gain is Clean-global; grouped candidate
  adapter is offline-only; HSL requires its own audit.
- `E-FI-2`: selected local scenarios carry `[7]` root artifact,
  `[H_max+1,29]` deployment-q29 intent, and `[K,65]` GMT-only Clean
  continuation under one immutable identity; the old reset path fails closed.
- `E-FI-3`: the offline actor bridge consumes only declared-offset q29 intent
  as `[B,|H|*29]`; Clean/root/global fields cannot alter the tail, and legacy
  layouts or unversioned checkpoint-like prefix statistics fail closed.
- `E-FI-4`: HSL warmup bypasses the q29 bridge; its current anti-DR label is
  current-frame privileged supervision, while the enabled rollout HSL label is
  a quartet/Clean global target that reaches the supervised loss.
- `E-FI-5`: H0-A retains only current-frame anti-DR proposal initialization,
  forbids the Clean-quartet rollout label and direct legacy checkpoint migration,
  and records conditional H1 acceptance evidence.
- `E-FI-6`: H1-S1a deterministic contracts prove the Stage-1 q29 route and
  target boundary, plus v015 Stage-3 HSL write/loss/checkpoint rejection.
- `E-FI-7`: fake owners prove the Stage-1 q29 actor/target connector and the
  v015 Stage-3 zero HSL-write/storage/batch/loss connector without an update.
- `E-FI-8`: fake reset proves two scored roles share one sealed `x_t`, artifact,
  q29, C, K, and hash without resampling; command rejects legacy roles/mixed
  carriers and generic future/K reads before Step 2B.
- `E-FI-9`: fake candidate route proves one t action, zero later repair,
  command-owned C[q29,dq29,root] at each K offset, exact K masks, one Repair
  policy tuple per scenario, and immutable re-arm for the next M attempt.
- `E-FI-10`: pure S1 Gain fixtures prove q29 I is the only fidelity target,
  no Clean/root/global input is representable, missing optional evidence is
  `NaN`, and existing paired K-Physics/full-6D cost semantics remain explicit.
- `E-FI-11`: deterministic candidate-only two-role capture proves post-`t`
  robot q29 and sealed deployment/Noisy `I[t]` reach v003 only; its return/
  advantage and scenario-keyed priority evidence preserve provenance, hash,
  decomposition, and fail-closed invalid rows without PPO or sampler mutation.
- `E-FI-12`: v003 candidate evidence projects into a local-K diagnostic report
  with q29 intent/provenance, physics, cost, total, identity, and K; composition
  remains a distinct no-feedback protocol, and all legacy v002 evaluators reject
  v015 before capture.
- `E-FI-13`: a sealed v003 candidate carrier becomes one v015 metadata row per
  Repair attempt and a complete grouped candidate batch. Scenario/hash/x_t/q29/
  K/evidence-step identity survives; old fixed-tape metadata and legacy PPO
  adapter reject; no formal update occurs.
- `E-FI-14`: an injected CPU fake route seals `2 Segment x 2 attempt` candidate
  shards against one frozen snapshot, routes q29 intent before normalization,
  executes unchanged grouped v003 loss, and proves one explicit optimizer step;
  partial/HSL/legacy paths fail before step.
- `E-FI-15`: established pre-R3 generic-dimension save/load and transaction
  atomicity mechanics; its exact-layout claim is rebased by R4 `E-FI-22`.

## Unresolved Risks

- generic checkpoint cadence/dispatch and actual resume remain untested; R4 is
  deterministic S3 evidence only.
- R5 closes the prior `_read_live_observations()` fake gap at offline S2, but
  simulator observation timing and the actual SUST_Main_2 route remain R6.
- H1 has no live scenario installation; current warmup still fails closed until
  a sealed local scenario is supplied by an authorized lifecycle route.
- legacy policy-quality/formal evaluator HSL branches remain outside H1;
  their label owner fails closed, but their route is not integration-tested.
- legacy HSL checkpoints lack the q29 layout identity and remain reject-only;
  a new persistence identity is still undefined.
- the old quartet/full-tape paths are isolated at the local command carrier,
  but the formal runner still owns legacy route selection.
- no generic v015 formal runtime, actual checkpoint cadence/resume, simulator,
  training, or live evidence exists; `E-FI-15` is only a CPU fake-S3
  persistence/atomicity proof.
- candidate priority is an immutable scenario-keyed artifact only; stable
  segment/trial assignment and sampler-state mutation remain outside Step 3B.
- Step 3C is deterministic protocol/diagnostic evidence only; no formal
  periodic evaluator, sequence executor, simulator timing, or deployed
  composition metric has been run.
- Step 4B/4C prove only the injected fake route, exact-one optimizer boundary,
  and temporary CPU checkpoint identity/atomicity. Generic formal dispatch,
  actual checkpoint cadence/resume, and live execution remain unconfirmed.
- `E-FI-17` confirms the current Codex host is Darwin arm64 without IsaacLab,
  NVIDIA runtime, or the server `/hdd1/cyx` artifacts. The live sentinel must
  run from a synchronized SUST_Main_2 workspace; no local fallback is valid.

## Next Step

After explicit informed authorization to transfer the named owner files to the
trusted SUST_Main_2 host, run exactly one bounded v015 transaction with the
structured identity/observation snapshot.
Require `870/58/928/158/770`, role/scenario/hash/q29/C identities, one FEMR
action, post-advance Clean-C K order, four equal-mass attempts, and update delta
one. Stop immediately on any missing identity, later FEMR action, mixed
reference, or update delta mismatch; do not start long training.
