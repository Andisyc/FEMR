# FrontRES Design Contract Entry

The active contract registry is:

```text
note/frontres_core/contracts/README.md
```

The active method contract is:

```text
note/frontres_core/contracts/active/method/FRS-METHOD-v015-future-intent-single-action-k-replay.md
```

The human-readable design-point source is:

```text
note/architecture/concept/03_frontres_concept_tabs.data.json
```

Its stable block IDs map to detailed active contract sections through the
Design Point Register in `note/frontres_core/contracts/README.md`.

This compatibility entry contains no historical design. Do not use it as a
contract body and do not scan `history/` by default.
